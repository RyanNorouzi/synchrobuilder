# Good

Written in python, packaged with pip. Both python and pip are mentioned only in prose here.

```sh
python3 -m venv .venv
source .venv/bin/activate   # macOS/Linux
```

```cmd
.venv\Scripts\activate
```
